CREATE TABLE Charts (record DATE, rank INT, song VARCHAR(255), artist VARCHAR(255), last-week INT, peak-rank INT,weeks-on-board INT,PRIMARY KEY(rank));

CREATE TABLE Charts (date,rank INt,song VARCHAR(255),artist VARCHAR(255),lastweek INT,peak-rank INt, weeksonboard INT, PRIMARY KEY(rank));


CREATE TABLE Charts (
    record DATE,
     ranking INT, 
     song VARCHAR(255), 
     artist VARCHAR(255), 
     lastWeek INT, 
     peakRank INT,
     weeksOnBoard INT,
     PRIMARY KEY(ranking)
     );

INSERT INTO billboard VALUES('')