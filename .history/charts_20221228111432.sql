CREATE TABLE charts (
    date DATE,
    rank INT AUTO_INCREMENT NOT NULL,
    song VARCHAR,
    artist va,
    last-week INT,
    peak-rank INT,
    weeks-on-board INT,
    PRIMARY KEY(rank)
    );